# Six qubit response execution study

Protocol recorded 14 September 2026 before calculations. This is an engineering diagnostic of the exact spectral circuit architecture in Project Pulsar v0.8.0, using the existing three-site triangle fixture with a nine-dimensional Gaussian operator. It is not a new biological prediction and does not establish original nonlinear fidelity. Inputs and circuit sources are copied without modification.

We will build all six unordered response entries at each of the three saved times, giving 18 direct Hadamard readouts, with no Grover amplification. Each circuit uses four eigenmode address qubits, one branch qubit and one attenuation flag. No probabilities may be directly encoded from the known classical answer. All nine spectral modes, observable loaders and attenuation controls must remain in the circuit. We will compile using Qiskit 2.3.1 with fixed seed 1729, optimization level 3, to CZ and arbitrary equatorial rotations. We will route onto the six connected IQM Garnet positions [7,8,9,10,13,14] and only the edges (7,8),(8,9),(9,10),(8,13),(9,14), which are explicitly documented by AWS in its Garnet launch example. These are a documented connectivity subset, not a fresh calibrated device snapshot. Current Amazon Braket documentation confirms Garnet support and native CZ/PRx gates. Fresh device topology, access, calibration and allocation are required before a physical run. No QPU task will be submitted here.

Every compiled circuit will be checked by local ideal statevector against the independent matrix exponential. Save all circuits, qubit mappings, compiled gate/depth counts and comparison outputs. Compute local density-matrix probabilities for explicitly assumed independent depolarizing channels, with two-qubit parameters 0, 0.00001, 0.0001, 0.001 and 0.01, single-qubit parameter one tenth the two-qubit parameter, and symmetric readout flip probabilities 0, 0.0001, 0.001, 0.01 and 0.01 respectively. These are sensitivity scenarios, not vendor calibration or physical measurements; no crosstalk, drift, leakage or relaxation is inferred. Include a separate readout-only scenario.

For 18 outputs, exact two-sided Clopper–Pearson intervals receive failure probability 0.05/18 each, yielding at least 95% joint coverage for independent identically distributed binomial readouts. Map the complete probability intervals into signed response intervals using known observable norms and the exact initial covariance subtraction. Include inherited Gaussian projection discrepancy, numerical compilation discrepancy and an explicitly separate physical bias allowance; sampling confidence alone cannot certify ideal response error. Calculate worst-case statistical precision for 16,384 and 65,536 shots per query and a sufficient shot ceiling for normalized total error 0.002 if the physical bias budget is 0.0005. Preserve both affordable diagnostic and final precision budgets, and do not lower the final scientific target implicitly.

A candidate diagnostic run uses 16,384 data shots per query, with four readout-only calibration circuits (both states before and after) at 8,192 shots each and three t=0 paired-observable identity controls at 16,384 shots each. Calibration results are used to detect readout drift; do not assume they remove circuit noise. The decision is whether simultaneous intervals enlarged by qualified bias fit 0.05 diagnostic error. Maintain the 0.002 end-point as a separate escalation criterion. All costs are calculations from the dated public AWS tariff, not reserved allocation or vendor quotes; task splitting and extra storage/taxes are stated.
