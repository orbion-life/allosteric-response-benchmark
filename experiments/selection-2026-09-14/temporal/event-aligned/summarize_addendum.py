from pathlib import Path
import json,datetime
ROOT=Path(__file__).resolve().parent
rows=[]
for target in ['kras','abl','myc','myh7']:
 x=json.loads((ROOT/'results'/target/'analysis.json').read_text());r=json.loads((ROOT/'results'/target/'receipt.json').read_text());pulses=[]
 for m in x['pulses']:
  high=[z for z in m['ranking'] if not z['low_signal']]
  pulses.append({'duration_over_tau':m['duration_over_tau'],'max_absolute_error':m['maximum_absolute_response_error'],'worst_post_removal_lag_over_tau':m['worst_post_removal_lag_over_tau'],'passing_times':m['passing_times'],'total_times':m['total_times'],'all_times_pass':m['all_times_pass'],'sign_disagreements':m['sign_disagreements'],'sign_eligible_entries':m['sign_eligible_entries'],'high_signal_times':len(high),'high_signal_min_top5_membership':min(z['top5_membership'] for z in high) if high else None,'high_signal_identical_top5_order_times':sum(z['top5_order_identical'] for z in high),'high_signal_gap_certifiable_times':sum(z['gap_above_0_004'] for z in high),'sampled_peak':m['peak']})
 rows.append({'target':target,'pulses':pulses,'independent_exponential_max_abs':max(z['independent_exponential_max_abs'] for z in x['independent_verification']),'GPU_seconds':r['seconds'],'GPU_saved_CPU_parity':r['saved_CPU_parity_max_abs']})
s={'created_utc':datetime.datetime.now(datetime.timezone.utc).isoformat(),'scope':'Separately frozen developmental event-aligned characterization; original grid retained unchanged','rows':rows,'raw_full_kernel_rule':'180 newly evaluated times plus 64 reused and hash-verified prior kernel times; old full-kernel arrays are immutable','all_pulse_curves_pass':all(p['all_times_pass'] for r in rows for p in r['pulses'])}
(ROOT/'summary.json').write_text(json.dumps(s,indent=2)+'\n')
lines=['# Early response errors recur after finite perturbations are removed','','The initial absolute-time grid under-sampled the interval immediately after long pulse removals. This separate addendum was declared and sealed before the added kernel values or response scores were calculated. It preserves the original protocol, time grid, measurements and failures. No covariance, model, representation, basis or response threshold was changed.','','Each duration (0.03, 0.3 and 3τ) now has 61 observations: exact removal and the original fixed logarithmic positive lags after removal, restricted to absolute t≤30τ. These use 180 new kernel times and 64 previous kernel times verified by SHA-256. The prior reference arrays remain unchanged.','','| Target | d=0.03τ: max error / passing points | d=0.3τ: max error / passing points | d=3τ: max error / passing points |','|---|---|---|---|']
for r in rows:lines.append('| '+r['target'].upper()+' | '+' | '.join(f"{p['max_absolute_error']:.6g} / {p['passing_times']}/{p['total_times']}" for p in r['pulses'])+' |')
lines+=['','These are maximum absolute signed matrix-entry errors in the fixed normalized response units, compared with the unchanged 0.002 threshold. The observation set starts at removal, so it directly measures the previously under-sampled post-removal response. The complete error arrays, sign counts and low-signal/gap-qualified rankings remain in `results/TARGET/analysis.json`.','','The signed pulse equals K(d+s)−K(s) in linear response. The two terms are evaluated at their fixed declared arguments; the stored time-closure roundoff is also reported. Newly sampled errors can therefore expose deficiencies in the short-lag K(s) term even when a sustained response at the later absolute time appears accurate. This is numerical representation error within a fixed Gaussian model, not evidence of biological scrambling.','','A source setup attempt failed before any new kernels because the direct CuPy eigensolver import did not locate libcusolver. Running the unchanged independent GPU fixture checks first, as in the successful original campaign, resolved the initialization issue. Both setup sources and the correction receipt are retained; the time/model protocol stayed unchanged.','','Peak fields retain the initial frozen labels. In prose they mean unique interior sampled maxima under the fixed 1e-12 tie rule. They are not error-certified or continuous-time peaks; low-amplitude, flat/tied and boundary cases remain separate. No behavior between sampling points is certified.','','Verification: `python verify_addendum.py --mode protocol --output protocol-audit.json` checks seals and exact grid construction without GPU work. With all original and addendum raw kernels and reconstructed waveforms present, `python verify_addendum.py --mode saved --output saved-verification.json` checks input hashes, full pulse identities, sign counts, rank arrays and error summaries. A passing implementation audit preserves the scientific failures.','','The compact public bundle omits redundant derived `pulse-*.npz` files with explicit hashes. From the temporal package root, `python replay_waveforms.py --output NEW_DIRECTORY` reconstructs the original and event-aligned waveforms in a separate tree and performs both saved-array audits. This does not refit proteins or repeat the GPU kernel campaign.']
(ROOT/'README.md').write_text('\n'.join(lines)+'\n')
print(json.dumps(s,indent=2))
